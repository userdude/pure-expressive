#!/usr/bin/env bash

# Suppress output
OUT="$(./play-worker)"

CONTAINER_PREFIX="$(basename "$(cd .. && pwd)")-$(basename "$(pwd)")"
EXPRESS_WORKER_NAME="${CONTAINER_PREFIX}_play-worker"

docker exec -it "${EXPRESS_WORKER_NAME}" console "$@"

echo ""
